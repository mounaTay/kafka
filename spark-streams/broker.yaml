apiVersion: v1
kind: Service
metadata:
  name: kafka-service-loadbalancer
  namespace: kafka
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 9092
  selector:
    app: kafka-broker


# apiVersion: v1
# kind: Service
# metadata:
#   name: kafka-service
#   namespace: kafka
#   labels:
#     name: kafka
# spec:
#   ports:
#   - port: 9092
#     name: kafka-port
#     protocol: TCP
#   selector:
#     app: kafka-broker
#     id: "0"
#   type: LoadBalancer